#include "Sys.h"
/***************************************************
*								ms延时子函数
*						 n=1时，约为1ms
****************************************************/
void delay_ms(uint16 n)
{
    uint16 x;
    while (n--)
    {
        x = 580;
        while (x--);
    }
}